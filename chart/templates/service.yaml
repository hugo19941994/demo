apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-anti-spoofer.fullname" . }}
  labels:
    {{- include "demo-anti-spoofer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "demo-anti-spoofer.selectorLabels" . | nindent 4 }}
